let userNames: string [] = ['admin','umair','ilyas','mubeen','faizan'];

if(userNames.length === 0){
    console.log('we need to find some users!');
}else{
    userNames = [];
    console.log('all user name have been removed .' + userNames.length);
}
